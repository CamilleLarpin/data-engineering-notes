
<!-- from: software-engineering/testing-ci-cd -->
# Notes — testing-ci-cd

test est une ou plusieurs condition a verifier

assert VS eval:
assert: evaluer condition
eval: transformer string en code


On test le plus tot possible:
Dans un monde ideal on fait du test driven development. 
Mais dans la pratique cela rajoute de la complexité - on a envie d'etre agile. 

different test:
unit test: 
    rendre le code atomic pour avoir un code plus facile à utiliser 
    modularité? est ce lié?
    atomic permet les test unitaire

integration test
    quand on met tout ensmeble bout a bout: est ce que les differentes brics interagissent bien les unes avec les autres
qualification test

unit test
context
dans in folder on a typiquement
- bin: range script sh
- lib: range les packages?
- module Class function
>> test unitaire sur les fonctions

project/
├── bin/
├── lib/
│   ├── data_loading.py
│   └── preprocessing.py
├── notebooks/
├── tests/
│   ├── conftest.py
│   ├── test_data_loading.py
│   └── test_preprocessing.py

Refactorisation de code base: factoriser en code atomic

On va utliser pytest pour faire nos test. 
On doit toujours savoir où le code s'execute pour gere les import relatif. Quandon installe le code comme un package, si on est n'importe où dans l'ordi mais qu'on est dans le bon environment, on a acces au package. 

pytest cherche tous les odisser où il y a le mot test et les execute là ou est le mot test. Mais bonne pratique est que le code doit etre executé au meme endroit - donc on utilise le python path.

Faire attention a toujours lancer les test a la racine du repo.

Si on a une liste de choses à tester on peut uitliser makr.parameteres ??

Comment tester une fonction qui a une depandance: par ex un fonction qui fait une requete api. 
On peut moker les outpus des appels externe. 
Ex: dans le script au lieu d aplliquer la fonction tu va le remplacer la dependance par un truc automatique. 

Comment creer un package TBC
- code source
- pyproject.toml 


concretement:
1. on cree un dossier test
2. dans le fichier test: très agreable de pouvoir importer en package le code à tester plutot que de faire un import locale. Pourquoi? TBC
3. je cree des fonctions qui test mes fonctions
naming: test_fonction_name

Makefile file: BAzzoka utiliser comme cheat shit de command
- command sans typo
- command type a utiliser
Makefile utile quand on a plusieurs fonction/workflow qui doivent se lance dans un ordre donné - via des dependance explicite fonction 1 : fonction 2 @echo ...

Make tests: va compiler le code a l'intereieur ??  TBC make tests
.Phony aide à comprendre ceci n'est pas un dossier

Si on fait un make file avec des variable d'environment... TBC
Makefile est que de la CLI >> 

Le but du cours d'ajourd hui est de mettre en place des tests et de les automtiser: comment on les automatise? TBC


Integration test
On veut se concetrer sur l'input et l'ouput et pas sur l enchainement des fonctions. 
Et plus de mocking et de patching

qualification test
a lancer dans preprod - avoir exactement le meme environmeent que la prod. lancer les tests automatiquement dans lenveironment de preprod
On test pas le code, pas la logique, mais seulmeent que ca marche. 

CICD pipeline
D'integration continue env dev
On va lancer les test automatiquement des que quelqu un fait une PR. On ne va pas se baser sur se faire confiance de faire les tests en locale. On va le bloquer à l'entree. 
Automated lifecycle.
continouis integration: atimatiser les bonnes practiques, les checekr, et permettre une collab coherente. 
What do we need?
git
outil d 'hebergement de repo
outil integration co ntinue: jenkins
set up un triger: push, PR, push avec tag?
Test unitaire et tests integration

Continous delivery env preprod
en plus de la pipleine automatise on a besoin d outil de ploiement automatique - TBC
Outil: terraform, Test qualification

continous deplyement env prod
Une fois que tout cela est passé - on deploit en prod. 
Validation manuelle pour lancer le deploiement. 
Tool depend du service sur lequel on deploie. Ex Kubernets. 

Terraform permet de definir infra dans des fichiers yaml. Gestion de l'infra as code. Gestionnaire de version. 


Dans git hub, on a des actions.
Dans ces actions on a des integration avec pylint, pytest, terraform, gcp incluse.
Si on click sur create dans les action on genere un yaml.
Au prealable dans .github il nous faut dossier worklfow dans lequel on mets ces fihcers d'automatisation. 
Installer extension github actions pour se reperrer dans le yaml. 
dans le yaml on a le trgier 'on', niveau de permision, list d'actions a faire definies dans job. 
on va definir sur quel worker on va lancer l'application (c'est quoi un worker?)
Ensuite on a une liste de steps. 

Good practice: mettre a jour reguleirement les commandes dans le makefile.

C'est quoi un working tree? c'est lie a git?

Nouvelle commande git: git commit --amend, puis git push -- force  mais attention avec command git push -- force
Dans git on peut mettre en place des regles pour empecher les certaines de commandes: ex empecher les force push, empecher d'ecrire sur le main driectement. 

Bonne pratique: reset l'envirnment en locale avant...  TBC


On retrouve toutes les actions de jobs dans l'interface de github dans actions et on peut repere exactement ce ui a failed et ce qui a reussi/. 

Les commandes Make ne doivent pas s'utiliser dans un pre-commit car cela crée une dépendance sur Make. Il faut utiliser les commandes directement.
